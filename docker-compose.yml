version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      - PYTHONPATH=/app/src
    ports:
      - "5000:5000"
    restart: unless-stopped
